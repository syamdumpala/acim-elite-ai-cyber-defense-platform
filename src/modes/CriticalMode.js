export default function CriticalMode() {
  return {
    background: "bg-gradient-to-br from-[#1f0a0a] to-[#111827]",
    accent: "text-red-500",
    glow: "#ef4444",
    overlay: "animate-pulse",
  };
}